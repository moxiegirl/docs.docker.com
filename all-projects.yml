defaults:
    org: docker
    ref: master
    path: docs
    repo_name: "{name}"
    name: "{repo_name}"
    target: "content/{name}"
    ignores: ['.*/Dockerfile']

projects:
    - name: docs-base
      org: docker
      ref: hugo-github-linking
      path: !!null
      target: .

    - name: engine
      repo_name: docker
      org: moxiegirl
      ref: flickerbox-testing
      path: docs/
